#include <stdio.h>
/**
 * main - Print all the letters except
 *
 * Return: Zero 0 (Success)
 */

int main(void)
{
	char c;
	c = 'a';

while
	(c <= 'z')
{
if (c != 'e' && c != 'q')
{
	putchar(c);
}
	c++;
}
	putchar('\n');
return (0);
}
