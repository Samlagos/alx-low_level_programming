#include <stdio.h>
/**
 * main - prints the alphABET
 * followed by a new line
 * Return: Always 0 (Success)
 */

int main(void)
{
char ch;
char c;

ch = 'a';
c = 'A';
	while
	(ch <= 'z')
{
putchar(ch);
ch++;
}
	while
	(c <= 'Z')
{
putchar(c);
c++;
}
putchar('\n');
return (0);
}
