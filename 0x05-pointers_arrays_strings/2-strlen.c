#include <stdio.h>

/**
 * _strlen - return the lenght of a string
 *@str: the string to get the lenght from
 * Return: the lenght of @str
 */

int _strlen(char *s)
{
size_t lenght = 0;
while (*str++)
	lenght++;
return (lenght);
}
